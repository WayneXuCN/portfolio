---
/**
 * 404.astro - 自定义 404 页面
 * Astro 会自动将此文件作为 404 错误页面
 * 注意：404 页面不包含 Footer，保持简洁
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import { getI18n, defaultLocale, type Locale } from '../lib/i18n';

// 404 页面使用默认语言
const lang: Locale = defaultLocale;
const translations = await getI18n(lang);
const { site } = translations;

// 404 页面文本（可以添加到 locale JSON 中）
const content = {
  zh: {
    title: '404 - 页面未找到',
    description: '抱歉，您访问的页面不存在',
    heading: '页面好像走丢了',
    message: '链接或许已更新，但好消息是，灵感仍在。试试看以下操作：',
    homeButton: '返回首页',
    contactButton: '联系 Wenjie',
    projectsTitle: '最近项目',
    projectsText: '浏览',
    projectsLink: '关于页',
    projectsEnd: '了解正在进行的实验与作品',
    contactTitle: '联系合作',
    contactText: '想和我聊聊？点此',
    contactLink: '发送邮件',
    contactEnd: '，我随时准备提供帮助',
  },
  en: {
    title: '404 - Page Not Found',
    description: 'Sorry, the page you are looking for does not exist',
    heading: 'Page seems lost',
    message:
      'The link may have been updated, but the good news is, inspiration remains. Try the following:',
    homeButton: 'Back to Home',
    contactButton: 'Contact Wenjie',
    projectsTitle: 'Recent Projects',
    projectsText: 'Browse the',
    projectsLink: 'About page',
    projectsEnd: 'to learn about ongoing experiments and works',
    contactTitle: 'Collaboration',
    contactText: 'Want to chat? Click here to',
    contactLink: 'send an email',
    contactEnd: ", I'm always ready to help",
  },
};

const t = content[lang] || content.zh;
---

<BaseLayout title={t.title} description={t.description} lang={lang}>
  <div class='min-h-[70vh] flex items-center justify-center py-20'>
    <div class='max-w-3xl mx-auto px-6 text-center'>
      <p class='text-xs uppercase tracking-[0.6em] text-red-500 font-semibold mb-6'>404</p>
      <h1 class='display-font text-5xl sm:text-6xl font-bold mb-6'>{t.heading}</h1>
      <p class='text-lg text-gray-600 dark:text-gray-400 mb-10'>
        {t.message}
      </p>
      <div class='flex flex-col sm:flex-row sm:justify-center gap-4 mb-12'>
        <a
          href={`/${lang}/`}
          class='px-6 py-3 bg-black dark:bg-white text-white dark:text-black rounded-full font-semibold card-hover transition-colors'
        >
          {t.homeButton}
        </a>
        <a
          href={`/${lang}/contact/`}
          class='px-6 py-3 border border-gray-300 dark:border-gray-600 rounded-full font-semibold card-hover hover:bg-gray-50 dark:hover:bg-gray-800 transition-colors text-gray-900 dark:text-gray-100'
        >
          {t.contactButton}
        </a>
      </div>
      <div class='grid sm:grid-cols-2 gap-6 text-left'>
        <article
          class='p-6 border border-gray-200 dark:border-gray-700 rounded-3xl card-hover bg-white dark:bg-gray-800'
        >
          <h3 class='text-xl font-semibold mb-2 text-gray-900 dark:text-gray-100'>
            {t.projectsTitle}
          </h3>
          <p class='text-gray-600 dark:text-gray-400 text-sm'>
            {t.projectsText}
            <a
              href={`/${lang}/about/`}
              class='underline ml-1 hover:text-black dark:hover:text-white'
            >
              {t.projectsLink}
            </a>
            {t.projectsEnd}
          </p>
        </article>
        <article
          class='p-6 border border-gray-200 dark:border-gray-700 rounded-3xl card-hover bg-white dark:bg-gray-800'
        >
          <h3 class='text-xl font-semibold mb-2 text-gray-900 dark:text-gray-100'>
            {t.contactTitle}
          </h3>
          <p class='text-gray-600 dark:text-gray-400 text-sm'>
            {t.contactText}
            <a
              class='underline ml-1 hover:text-black dark:hover:text-white'
              href='mailto:wenjie.xu.cn@outlook.com'
            >
              {t.contactLink}
            </a>
            {t.contactEnd}
          </p>
        </article>
      </div>
    </div>
  </div>
</BaseLayout>
